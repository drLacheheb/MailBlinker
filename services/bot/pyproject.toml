[project]
name = "bot"
version = "0.1.0"
description = "Telegram Bot Service for Gmail Tracker & Notifier"
requires-python = ">=3.11"
dependencies = [
    "core",
    "formatter",
    "aiogram>=3.4.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
